#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, i, total;
    printf("Deseja a tabuada para qual valor? ");
    scanf("%d", &n);

    total = 0;

    for(i = 1; i <= 10; i++){
        total = n * i;
        printf("%d x %d = %d\n", n, i, total);
    }
    return 0;
}
